# 开发决策记录：RAG 搭建（本地离线闭环）

> 记录"两个阻塞问题的解法 + 环境事实 + 当前状态"，供团队后续对接参考。

## 一、两个问题的解法

### 问题 1：本机开发拿不到公司知识库

**结论：数据获取不应阻塞 RAG 开发。** 方案 = 数据源适配器 + 结构等价的脱敏样例库。

- 数据源适配层（`src/rag/datasources/`）：`KnowledgeSource` 接口只负责
  「枚举文档 + 打开文件流」，下游解析/分块/索引/检索/工具代码与数据源完全解耦；
- 本地开发用 `LocalDirectorySource` 读自编"云雀科技有限公司"样例库
  （`scripts/generate_sample.py` 生成，多格式 + 部门/密级元数据）；
- 公司真实知识库按形态实现 `KnowledgeSource`（`company_stub.py` 占位），
  拿到数据后改 `config.yaml` 的 `datasource.type` 重新建库即可，业务代码零改动；
- 权限模型用样例库的伪造 `department` / `confidentiality` 即可完成
  `MetadataFilters` 联调（评测里带 4 条权限用例，含越权泄漏断言）。

### 问题 2：aioncore 后端 hermes 不支持 MCP

**结论：按"独立 RAG 项目先行"推进（已确认）。**

- 已核实 `AionCore\crates\aionui-mcp\src\adapters\` 只有
  aionrs/aionui/claude/codebuddy/codex/gemini/opencode/qwen，**无 hermes**；
- 本仓库把 RAG 做成独立 MCP Server（stdio），先脱离 aioncore 自测
  （`scripts/mcp_smoke.py` 直接走 stdio JSON-RPC）；
- 后续阶段：aionui-mcp 增加 hermes 适配器 → 注册 stdio MCP Server →
  前端 ToolsSettings 管理 → 用户身份（部门/密级）透传。

## 二、环境事实与踩坑（本机实测）

| 项 | 状态 |
| --- | --- |
| Python | 3.13.5（Anaconda 全局），uv 0.11.28；**uv 全局缓存 `AppData\Local\uv\cache` 权限异常** → 用 `UV_CACHE_DIR` 指到工作区 `.uv-cache` |
| pypi | 可达 ✓ |
| HuggingFace | 直连部分可达（走系统代理）；**不要设 HF_ENDPOINT 镜像**（hf-mirror 308 回源触发 hf_hub 1.28 `FileMetadataError`）；需 `HF_HUB_DISABLE_XET=1` + `HF_HOME` 指工作区 |
| 0700 目录陷阱 | 本机对 mode=0700 目录收紧 ACL 后不可写（tempfile.mkdtemp / hf_hub 下载事务全中招）→ 内置 `_winfix.py` 补丁 |
| Ollama | **未安装**（`winget install Ollama.Ollama` + `ollama pull qwen2.5:7b`，装完开 `config.yaml` 的 `llm.enabled`） |
| Embedding | v1 用 jinaai/jina-embeddings-v2-base-zh（fastembed/ONNX，中英混合，CPU 可跑）。fastembed 0.8 不支持 bge-m3；升级 bge-m3 = config 一行（huggingface 后端 + torch），代码零改动 |
| BM25 | 官方 llama-index-retrievers-bm25 对中文无效（空白分词）→ 自研 `ChineseBM25Retriever`（CJK 二元组 + bm25s，零依赖） |
| FastMCP | mcp 2.x 起为独立包 `fastmcp`（`from fastmcp import FastMCP`） |
| 中文控制台 | 统一 UTF-8 输出（rag/__init__.py reconfigure） |

## 三、当前状态与路线图

- [x] 项目脚手架 + 数据源适配器 + 样例生成器 + 建库/检索/MCP/评测代码
- [x] 依赖安装（uv sync）+ 本机环境适配（0700 目录补丁 / HF Hub / 中文 BM25 / fastmcp）
- [x] **端到端最小闭环**：生成样例库 → 建库 → 检索自测 → MCP 冒烟 → 评测
      （hit_rate 100% / mrr 0.9531 / 权限用例 0 泄漏）
- [x] **Ollama + LLM 增强已启用**：Ollama 0.33.0 + qwen2.5:7b（RTX 3060 6GB，实测 9 tok/s）；
      三个 Extractor 中文 prompt 化；增强后 hit_rate 100% / **mrr 1.0000**；
      `rag_answer`（检索+生成+引用）经 MCP 冒烟验证通过
- [x] **Q2Q 索引已接入**：预设问题 → 独立问题向量索引（`.data/index_q2q`）；
      三路召回（向量+Q2Q+中文BM25）；Golden Set 扩到 20 条（含 4 条问法差异用例）
      hit_rate 100% / mrr 1.0000
      （踩坑：0.14 的 TextNode 用 `id_` 字段，`node_id=` 静默失效导致 source_chunk_id 失配，已修）
- [x] **父子块已接入**：HierarchicalNodeParser(1536/512) → 8 父块 + 12 子块；
      子块入检索（三路融合不变）、父块经 `parents.jsonl` 挂接为 `parent_text` 作答上下文；
      `rag_answer` 重写：走 ACL 过滤 + 父块上下文 + 来源引用
      （0.14 的 AutoMergingRetriever 只接受 VectorIndexRetriever，无法包三路融合 → 用挂接方案）
- [x] **融合模式调优**：RRF 实测会淹没单路强命中（BM25 rank1 被多路中位的干扰文档压过，
      「常用系统有哪些」脱靶）→ 默认改 `fusion_mode: simple`（20/20，mrr 1.0000），
      relative_score/dist_based 次之；配置一行可切回 rrf
- [x] **Golden Set 扩到 51 条**（常规31/问法差异6/跨文档4/多跳4/干扰6，带 type 字段，
      评测报告分类型统计）：hit_rate 100% / mrr 0.9771；
      **干扰类 mrr 0.8056 为唯一短板**（「研发新功能前置」「产品清单」正确文档被语义
      相近文档压到 rank 2-3）——作为后续 Rerank 的 A/B 基准
- [x] **上下文压缩已接入**（`retrieve/compress.py`）：检索后按查询相关性压缩每块，
      两种模式——`embedding`（分句+相似度+按比例保留+恢复顺序，零 LLM，省 ~70-75% token，
      答案质量不降）与 `llm`（qwen 摘录原句，1152 字→139 字精度最高）；
      压缩只作用于 LLM 上下文，不改变检索排名（评测 hit_rate/mrr 不受影响）；
      踩坑：结构性过滤误删 markdown 表格行会丢答案（制度手册 500/350/800 就在表格里），已修
- [x] **时效感知已接入**（`retrieve/recency.py`）：时间作为关键属性，同族多版本"取新不取旧"。
      样例库新增《公司制度手册（2025版）》（住宿 400/300/600、报销周期 10 天，与 2026 版冲突）；
      非对比查询只保留最新版（prefer_latest），对比类查询（标记词：变化/以前/变了吗/这两年等）
      保留全部版本+旧版衰减（decay 0.5）；Golden Set 新增"时效"类型 3 条（其中对比类用
      require_all 断言两版都在）；54 条 hit_rate 100% / mrr 0.9784，原 51 条无退化。
      踩坑：0.14 内置 FixedRecencyPostprocessor 是"全局按 date 截断"不做 family 去重 → 自研；
      口语化对比问法（"变了吗/这两年"）需入标记词表（初版漏了导致对比放宽失效，已修）
- [x] **HTML 文档解析已接入**（`ingest/html_parser.py`，bs4 零额外依赖）：标题→Markdown 标题、
      表格→Markdown 表格（**不丢表格**）、列表→项目符号，丢弃 nav/footer/aside/form 等页面噪声；
      样例库新增《云雀云服务帮助中心.html》（FAQ+套餐对比表+导航噪声），解析质量 7/7 断言通过；
      Golden Set 扩到 58 条（+4 HTML 用例，含跨文档 5TB）；hit_rate 100%
- [x] **融合分数归一化**：三路召回融合前各除以本路最大值（`_ScoreNormRetriever` 包装器）。
      原因：BM25 原始分（0-3+）尺度远大于向量/Q2Q（0.3-0.5），SIMPLE 直接求和被 BM25 主导，
      宽泛文档（FAQ 高频"如何"）被过度抬升（「加班如何申请」误排帮助中心第一）；
      效果：干扰类 mrr 0.8056 → 1.0 修复，58 条 hit_rate 100% / mrr 0.9511；
      残留掉位是语义歧义排序（计费方式/文档协作能力等）——Rerank 候选基准
- [x] **Rerank 已接入**（`retrieve/rerank.py`，自研 `LLMNodeReranker`）：融合→ACL→时效→Rerank→top_k；
      qwen2.5:7b temperature=0 专用实例对候选打分排序；**补足机制**（LLM 只选几条时按融合序
      补到 top_k，rerank 只改序不降召回——补足前 hit 98.3%，补足后 100%）；
      58 条 hit_rate 100% / **mrr 0.9511 → 0.9655**；
      踩坑①：0.14 内置 LLMRerank 对 chat model（Ollama）走 context_messages 聊天模板，
      自定义纯文本 prompt 错配致提示词残缺 → 自研（自行拼候选+解析 "Doc: N, Relevance: M"）；
      踩坑②：temp 0 下 Ollama 跨进程仍偶发不绝对确定 → 补足机制兜底召回；
      flag_embedding 模式（bge-reranker-v2-m3）预留，服务器/算力环境接入
- [ ] 阶段三：AionCore hermes MCP 适配 + 前端 + 真实数据源
- [ ] 阶段四：可观测（Phoenix/LlamaTrace）、A/B、CI 回归

对照设计方案 §8：**阶段一、阶段二（含 Rerank）全部完成**——LLM 增强 + Q2Q + 父子块 +
上下文压缩 + 时效感知 + HTML 解析 + 融合归一化 + 评测基线（58 条，hit 100% / mrr 0.9655）。
阶段三（AionCore 接入）与阶段四（可观测/CI）待推进。
