# 部署到公司电脑（数据不出内网的运行指南）

> 场景：公司文档只存在于公司电脑、不能出公司；代码通过 GitHub 传递（本机 push → 公司电脑 pull）。
> 原则：**代码 + 公开模型（嵌入模型、qwen）可进出公司；公司文档永远留在公司电脑上**。
> 模型是公开资产，从本机拷到公司电脑**不违反"文档不出公司"约束**，反而是离线部署最省事的方式。

---

## 一、总体策略（关键）

系统设计上 **LLM（Ollama）是可选环节**，检索闭环不依赖它：

| 环节 | 是否需要 Ollama | 说明 |
| --- | --- | --- |
| `build_kb`（不开增强） | ❌ 不需要 | 解析→分块→向量化→索引，全离线 |
| `query` / `run_eval` / MCP `retrieve` | ❌ 不需要 | 检索全链路可用 |
| `build_kb`（增强：摘要/预设问题/关键词） | ✅ 需要 | LLM 生成 |
| MCP `rag_answer` | ✅ 需要 | 检索+生成 |

所以公司电脑可以分两步走：**第一步先跑通检索闭环（不装 Ollama，几分钟）**，第二步再装 Ollama 补齐生成能力。

---

## 二、代码与数据边界

| 项 | 本机（开发机） | 公司电脑 |
| --- | --- | --- |
| 代码（git 仓库） | push | pull |
| 嵌入模型 `.data/models`（~1.2GB，公开模型） | 打包拷贝（可选，离线更快） | 放到 `rag\.data\models` |
| Ollama 安装包 + qwen 模型（公开模型，~6GB） | 打包拷贝（可选） | 安装/放置 |
| **公司文档** | **不放** | **只留在公司电脑**（替换 sample_data） |
| 建库产物 `.data/index` | 不拷贝（公司文档建库结果，随公司文档走） | 公司电脑上重新 `build_kb` 生成 |

---

## 三、步骤 A：拉代码 + 装依赖

```powershell
git clone <你的仓库地址>          # 或已在仓库里则 git pull
cd rag

# 装 uv（公司网络能访问 pypi 时）：
#   winget install astral-sh.uv    或    pip install uv
# 公司网络禁外网时：从本机拷贝 uv 的 windows x64 单文件 exe 到公司电脑并加入 PATH
uv --version

# 安装依赖（需要 pypi 可达；公司网络禁 pypi 时见附录「离线依赖」）
uv sync
```

> uv 会在项目内创建 `.venv` 并按 `uv.lock` 锁定版本；`pyproject.toml` 的
> `cache-dir = ".uv-cache"` 让缓存落在项目内，不需要改任何路径。
> 已推送到 git 的代码里**不再有本机绝对路径**（`.env` 已删除、`run.ps1` 已可移植，
> 环境变量默认值由 `config.py` 按项目目录自动推导）。

---

## 四、步骤 B：嵌入模型（二选一）

**方式 1：在线下载**（公司电脑能访问 huggingface.co 或可配代理时）

```powershell
uv run python -m rag.scripts.dl_model
```

**方式 2：离线拷贝（推荐，模型是公开的）**

在本机打包（模型 + HF 缓存一起带）：

```powershell
# 本机执行：压缩模型缓存
Compress-Archive -Path rag\.data\models -DestinationPath embedding-models.zip   # ~1.2GB
```

拷到公司电脑后解压到对应位置：

```powershell
# 公司电脑执行
Expand-Archive -Path embedding-models.zip -DestinationPath rag\.data\ -Force
# 结果应为 rag\.data\models\models--jinaai--jina-embeddings-v2-base-zh\...
```

验证：`uv run python -m rag.scripts.dl_model`（模型已在缓存中，会秒过不下载）。

---

## 五、步骤 C：跑通检索闭环（先不装 Ollama）

```powershell
# 1. 关闭 LLM（公司电脑暂不装 Ollama 时）
#    config.yaml 里改为：
#      llm.enabled: false
#      enhance.enable_summary: false
#      enhance.enable_questions: false
#      enhance.enable_keywords: false

# 2. 配置数据源指向公司文档目录（见步骤 D）

# 3. 建库（跳过增强，仅解析→分块→向量化→索引）
uv run python -m rag.scripts.build_kb

# 4. 检索自测 / 评测 / MCP 冒烟
uv run python -m rag.scripts.query --query "你的测试问题" --top-k 3
uv run python -m rag.scripts.run_eval        # 注意：现有 51 条 Golden Set 是针对样例库写的，
                                             # 公司文档需按设计方案 §7.1 重写（真实问题+专家标注）
uv run python -m rag.scripts.mcp_smoke       # 此时 rag_answer 会返回"LLM 未启用"提示，属预期
```

> 检索闭环不依赖 LLM：三路召回（向量+Q2Q+BM25）、权限过滤、父块、压缩全部可用；
> 只是没有"摘要/预设问题/关键词"增强（Q2Q 索引会跳过）和生成式回答。

---

## 六、步骤 D：把数据源切到公司文档

`config.yaml`：

```yaml
datasource:
  type: local_dir
  params:
    root: D:\公司文档库          # 公司文档目录（绝对路径最稳；支持 md/pdf/docx/pptx/txt）
    manifest: D:\公司文档库\manifest.csv
```

**必须建 `manifest.csv`**（否则所有文档默认 `department=unknown, confidentiality=public`，
权限过滤形同虚设）。列：

```csv
path,doc_id,title,department,confidentiality,doc_type
制度手册/考勤管理办法.pdf,policy_attendance,考勤管理办法,行政部,internal,pdf
产品部/2026规划.pptx,prod_plan_2026,2026年产品规划,产品部,secret,pptx
...
```

- `path`：相对 `root` 的路径；`confidentiality`：`public | internal | secret`；
- 部门/密级是 ACL 过滤的依据（部门匹配 **或** public），按真实权限填；
- 建好后 `uv run python -m rag.scripts.build_kb` 重新建库（无需重装任何东西）。

---

## 七、步骤 E（可选，第二步）：装 Ollama 补齐生成能力

**方式 1：离线携带（推荐，模型是公开的）**

本机打包：

```powershell
# 1) Ollama 安装包（本机曾下载，或从 ollama.com 重新下载）
# 2) qwen 模型目录（~4.7GB）：
Compress-Archive -Path $env:USERPROFILE\.ollama\models -DestinationPath ollama-models.zip
```

公司电脑执行：

```powershell
OllamaSetup.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART   # 静默安装
# 若安装时已自动启动 ollama，先退出托盘再替换模型：
# 把 ollama-models.zip 解压到 %USERPROFILE%\.ollama\models（覆盖/合并）
# 启动 Ollama（app 或 ollama serve）
ollama list        # 应看到 qwen2.5:7b
```

**方式 2：公司网络可达时直接拉取**

```powershell
ollama pull qwen2.5:7b
```

然后开启生成能力：

```yaml
# config.yaml
llm.enabled: true
enhance.enable_summary: true
enhance.enable_questions: true
enhance.enable_keywords: true
```

重新建库启用增强（Q2Q 索引等自动生成）：`uv run python -m rag.scripts.build_kb`；
之后 `rag_answer` 即可检索+生成。

---

## 八、附录：离线依赖（公司电脑禁 pypi 时）

在本机把依赖 wheel 打包（供公司电脑离线安装；wheels 分平台/分 Python 版本，
**两台机器用相同 Python 版本**最稳，本机 venv 是 3.11）：

```powershell
# 本机执行（在 rag 目录，先 uv sync 建好 venv）
uv export --format requirements.txt -o requirements-offline.txt
uv pip download -r requirements-offline.txt -d wheels\
```

公司电脑离线安装：

```powershell
# 公司电脑：先有 Python 3.10+（没有则连 Python 安装包一起带；建议与开发机同版本）
uv venv
uv pip install --find-links wheels\ -r requirements-offline.txt
uv pip install -e .            # 安装项目本身（可省略，PYTHONPATH 指向 src 亦可）
```

> 备选：若公司电脑已有 Python，也可直接 `python -m venv .venv` + 离线 pip install wheels；
> 注意 `.venv` 是机器相关的，不要跨机器拷贝 `.venv`。

---

## 九、验证清单

| 检查项 | 命令 | 预期 |
| --- | --- | --- |
| 依赖就绪 | `uv run python -c "import llama_index.core"` | 无报错 |
| 嵌入模型就绪 | `uv run python -m rag.scripts.dl_model` | 秒过（缓存命中） |
| 建库成功 | `uv run python -m rag.scripts.build_kb` | 输出分块数/索引路径 |
| 检索可用 | `uv run python -m rag.scripts.query --query "..."` | 返回带来源片段 |
| 权限生效 | 用不同 department 查同一问题 | 结果随可见范围变化 |
| MCP 可用 | `uv run python -m rag.scripts.mcp_smoke` | 三工具注册，retrieve 正常 |
